# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This directory will contain a mobile application project. This file should be updated once the codebase is initialized.

## Build and Development Commands

**To be added once project is initialized.**

Examples based on common mobile frameworks:
- React Native: `npm install`, `npm start`, `npm run android`, `npm run ios`
- Flutter: `flutter pub get`, `flutter run`, `flutter test`
- Native Android: `./gradlew build`, `./gradlew assembleDebug`
- Native iOS: `xcodebuild` or open in Xcode

## Architecture

**To be documented once the project structure is established.**

Key areas to document:
- Project type (React Native, Flutter, Native Android/iOS, etc.)
- State management approach
- Navigation structure
- API integration patterns
- Data persistence strategy
- Authentication flow

## Testing

**To be added once testing framework is set up.**

Include:
- How to run unit tests
- How to run integration tests
- How to run a single test file
- Testing conventions

## Platform-Specific Notes

### Android
- Build configuration location
- Key Gradle commands
- Emulator setup notes

### iOS
- Build configuration location
- Pod installation steps
- Simulator setup notes

## Environment Setup

**To be documented.**

Include:
- Required environment variables
- Configuration files that need to be created
- API keys setup (without including actual keys)
- Platform SDK requirements
